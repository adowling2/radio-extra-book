% Notation macros. Deliberately short: a macro used once or twice hides the
% notation from anyone editing the source, so only the genuinely pervasive ones
% live here. (Twelve unused macros were removed on 2026-07-27.)
\newcommand{\jj}{\mathrm{j}}          % imaginary unit, upright per ISO 80000-2
\newcommand{\ee}{\mathrm{e}}          % Euler's number, upright
\newcommand{\dd}{\mathrm{d}}          % differential d, upright
\newcommand{\dB}{\mathrm{dB}}
\newcommand{\RMS}{\mathrm{RMS}}
\newcommand{\state}{\mathbf{x}}       % state vector
\newcommand{\mat}[1]{\begin{bmatrix}#1\end{bmatrix}}
